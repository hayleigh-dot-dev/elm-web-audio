{
  "scripts": {
    "example": "parcel example/index.html",
    "deploy": "parcel build example/index.html --public-url /elm-web-audio && gh-pages -d dist"
  },
  "devDependencies": {
    "elm-hot": "^1.0.1",
    "gh-pages": "^2.0.1",
    "node-elm-compiler": "^5.0.3",
    "parcel-bundler": "^1.12.3"
  }
}
